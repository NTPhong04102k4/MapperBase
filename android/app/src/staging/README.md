# Firebase config — flavor `staging`

Đặt `google-services.json` của Firebase project staging vào thư mục này.

Package name trong Firebase console: **`com.mapper.staging`**.

Chi tiết cách plugin chọn file và cách khai SHA-1/SHA-256: xem
`android/app/src/dev/README.md`.
